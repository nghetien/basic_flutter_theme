import 'package:flutter/material.dart';

class BasicInputTypeAhead extends StatefulWidget {
  const BasicInputTypeAhead({Key? key}) : super(key: key);

  @override
  State<BasicInputTypeAhead> createState() => _BasicInputTypeAheadState();
}

class _BasicInputTypeAheadState extends State<BasicInputTypeAhead> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder(

    );
  }
}
